import { Request, Response, NextFunction } from 'express';

export default class MediaController {
    constructor() { }

    public getMedia = (req: Request, res: Response, next: NextFunction) => {};
    public getMediaById = (req: Request, res: Response, next: NextFunction) => {};
}